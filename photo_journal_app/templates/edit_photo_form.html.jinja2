{% block edit_photo_form %}
    <form id="test-form"
          hx-post="/edit-photo"
          hx-trigger="submit"
          hx-target="#photo-edit-fields-{{ photo.doc_id }}"
          hx-swap="innerHTML"
          method="post">
        <input type="hidden" name="photo_id" value="{{ photo.doc_id }}">
        <textarea name="entry" class="w-full p-2 border border-blue-600 rounded">{{ photo.entry|trim }}</textarea>
        <button type="submit" class="mt-4 rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">
            Save
        </button>
    </form>
    <script>
    document.getElementById('test-form').addEventListener('submit', function(e){
        e.preventDefault();
        console.log('form submit triggered');
    });
</script>
{% endblock %}

